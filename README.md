# concierge
Multi-tenant AI SaaS — businesses embed a tenant-isolated concierge agent that retrieves from their own content, captures leads, and escalates to a human. The wall between tenants is the core guarantee.
